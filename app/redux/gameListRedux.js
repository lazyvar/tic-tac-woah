const types = {

}

export const actionCreators = {

}

const initialState = {
  games: []
}

export const reducer = (state = initialState, action) => {
  const {type, payload} = action

  switch(type) {
    default: {
      return state
    }
  }
}